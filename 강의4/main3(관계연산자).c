//관계연산자
//수학에서와 동일하지만
// != 는 다르다로 사용
// 컴퓨터에서는 !을 다르다로 씀
// 숫자를 인식할 때 0을 거짓으로 받아들임, 
//그 외는 참으로 받아들임

//출력시 거짓은 0, 참은 1
/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d\n", a > b);
	
	//큰따옴표 자체를 출력 \"
	//탭 (멀어지기) \t

	system("pause");

}	*/
//관계연산자를 통해 비교 가능