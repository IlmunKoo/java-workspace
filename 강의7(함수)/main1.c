//다양한 프로그래밍 기법들: 생산성 향상에 목적
//#함수
/*
#include <stdio.h>

//말머리 출력하는 함수, 특별한 값 반환x=>void
//어떠한 문장 반복하고 싶으면 함수로 빼내서
//처리하는 것이 더 깔끔
//함수화 하는 습관 들일 것!
//함수를 많이 만들고, main에서는 이를 
//불러오게 하는 것이 잘 짜여진 코드
void dice(int input) {
	printf("일문이가 던진 주사위 : %d\n", input);

}

int main(void) {
	dice(3);
	dice(5);
	dice(1);
	system("pause");

}

//#더하기 함수
#include <stdio.h>

int add(int a, int b) {
	return a + b;
}

int main(void) {
	printf("%d\n", add(10, 20));
	//%d는 형식지정자, 정수로 출력
		
}

//#사칙연산 함수
#include <stdio.h>

void calculator(int a, int b) {
	printf("%d + %d = %d\n", a, b, a + b);
	printf("%d - %d = %d\n", a, b, a - b);
	printf("%d * %d = %d\n", a, b, a * b);
	printf("%d / %d = %d\n", a, b, a / b);
	printf("\n");
}
	int main(void) {
		calculator(10, 3);
		calculator(17, 4);
		calculator(10, 3);
			system("pause");
	}*/


